# Handwritten Digit Recognizer
A CNN model trained on the MNIST dataset to recognize handwritten digits with ~99% accuracy.

## Libraries Used
- PyTorch
- torchvision
- matplotlib

## How to Run
pip install torch torchvision matplotlib numpy
python mnist_pytorch.py
